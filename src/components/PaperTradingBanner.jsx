/**
 * Banner — always visible at the top of the app.
 * Uses position: relative so it doesn't cover other UI elements.
 */
export default function PaperTradingBanner() {
  return null; // Removed — was covering UI elements
}
